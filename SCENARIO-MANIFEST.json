{
  "scenario": {
    "name": "breaking-type-change",
    "branch": "scenario/breaking-type-change",
    "description": "Breaking: token field type changed from string to integer in auth.jwt. Type changes are always classified as breaking."
  },
  "expected": {
    "exitCode": 1,
    "driftClass": "breaking",
    "reviewRequired": true
  }
}
