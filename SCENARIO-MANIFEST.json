{
  "scenario": {
    "name": "breaking-required-field",
    "branch": "scenario/breaking-required-field",
    "description": "Breaking: expiresAt removed from auth.jwt required fields. api.GET/search and event.user-action both import auth.jwt and are flagged at depth 1."
  },
  "expected": {
    "exitCode": 1,
    "driftClass": "breaking",
    "reviewRequired": true
  }
}
